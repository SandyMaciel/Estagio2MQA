<%= form_for(@doacao_material) do |f| %>
  <% if @doacao_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doacao_material.errors.count, "error") %> prohibited this doacao_material from being saved:</h2>

      <ul>
      <% @doacao_material.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :instuicao_id %><br>
    <%= f.number_field :instuicao_id %>
  </div>
  <div class="field">
    <%= f.label :quantidade %><br>
    <%= f.text_field :quantidade %>
  </div>
  <div class="field">
    <%= f.label :descricao %><br>
    <%= f.text_area :descricao %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
